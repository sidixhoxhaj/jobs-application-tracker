// Custom Fields Page
// Configure custom fields for job applications

const CustomFieldsPage = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Custom Fields</h1>
      <p style={{ marginTop: '0.5rem' }}>Field configuration will be displayed here</p>
    </div>
  );
};

export default CustomFieldsPage;
